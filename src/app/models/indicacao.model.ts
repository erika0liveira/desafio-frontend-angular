export default interface Indicacao{
    nome?: string,
    corretor?: string,
    status?: string,
    telefone?: string,
    empresa?: string,
    email?: string,
    id?: number,
    check?: boolean
}